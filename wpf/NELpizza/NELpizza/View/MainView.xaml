<Window x:Class="NELpizza.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:NELpizza.View"
        xmlns:viewmodels="clr-namespace:NELpizza.ViewModels.Views"
        xmlns:mainvm="clr-namespace:NELpizza.ViewModels"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Style="{StaticResource MaterialDesignWindow}"
        mc:Ignorable="d"
        Title="Custom Model Manager" Height="700" Width="1000">

    <Window.Resources>
        <DataTemplate
        DataType="{x:Type viewmodels:LoginViewModel}"
        >
            <local:LoginView/>
        </DataTemplate>


        <DataTemplate 
        DataType="{x:Type viewmodels:ParentItemViewModel}"
        >
            <local:ParentItemView/>
        </DataTemplate>

        <DataTemplate 
        DataType="{x:Type viewmodels:BestellingOntvangenViewModel}"
        >
            <local:BestellingOntvangenView/>
        </DataTemplate>

        <DataTemplate 
        DataType="{x:Type viewmodels:GebruikersBeheerViewModel}"
        >
            <local:GebruikersBeheerView/>
        </DataTemplate>

        <DataTemplate 
        DataType="{x:Type viewmodels:PizzaBeheerViewModel}"
        >
            <local:PizzaBeheerView/>
        </DataTemplate>

        <DataTemplate 
        DataType="{x:Type viewmodels:BezorgingViewModel}"
        >
            <local:BezorgingView/>
        </DataTemplate>


    </Window.Resources>

    <Window.DataContext>
        <mainvm:MainViewModel/>
    </Window.DataContext>
    

    <DockPanel>
        <!-- Navigation Menu -->
        <StackPanel DockPanel.Dock="Left" Background="#2C3E50" Width="200">
            <Button Content="Login" Command="{Binding NavigateCommand}" CommandParameter="LoginViewModel" Margin="10"/>
            <Button Content="Parent Items" Command="{Binding NavigateCommand}" CommandParameter="ParentItemViewModel" Margin="10"/>
            <Button Content="Block Types" Command="{Binding NavigateCommand}" CommandParameter="BestellingOntvangenViewModel" Margin="10"/>
            <Button Content="Klants" Command="{Binding NavigateCommand}" CommandParameter="GebruikersBeheerViewModel" Margin="10"/>
            <Button Content="PizzaBeheer Items" Command="{Binding NavigateCommand}" CommandParameter="PizzaBeheerViewModel" Margin="10"/>
            <Button Content="Bezorging" Command="{Binding NavigateCommand}" CommandParameter="BezorgingViewModel" Margin="10"/>
        </StackPanel>

        <!-- Dynamic Content -->
        <ContentControl Content="{Binding CurrentView}" Margin="10"/>
    </DockPanel>
</Window>