<Window x:Class="NELpizza.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:NELpizza.View"
        xmlns:viewmodels="clr-namespace:NELpizza.ViewModels.Views"
        xmlns:mainvm="clr-namespace:NELpizza.ViewModels"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Style="{StaticResource MaterialDesignWindow}"
        mc:Ignorable="d"
        Title="Custom Model Manager" Height="700" Width="1000">

    <Window.Resources>
        <DataTemplate
        DataType="{x:Type viewmodels:BakkerViewModel}"
        >
            <local:BakkerView/>
        </DataTemplate>


        <DataTemplate 
        DataType="{x:Type viewmodels:BezorgerViewModel}"
        >
            <local:BezorgerView/>
        </DataTemplate>

        
        
        <DataTemplate 
        DataType="{x:Type viewmodels:ManagerViewModel}"
        >
            <local:ManagerView/>
        </DataTemplate>


    </Window.Resources>

    <Window.DataContext>
        <mainvm:MainViewModel/>
    </Window.DataContext>
    

    <DockPanel>
        <!-- Navigation Menu -->
        <StackPanel DockPanel.Dock="Left" Background="#2C3E50" Width="200">
            <Button Content="Bakker" Command="{Binding NavigateCommand}" CommandParameter="BakkerViewModel" Margin="10"/>
            <Button Content="Bezorger" Command="{Binding NavigateCommand}" CommandParameter="BezorgerViewModel" Margin="10"/>
            <Button Content="Manager" Command="{Binding NavigateCommand}" CommandParameter="ManagerViewModel" Margin="10"/>
        </StackPanel>

        <!-- Dynamic Content -->
        <ContentControl Content="{Binding CurrentView}" Margin="10"/>
    </DockPanel>
</Window>