<UserControl x:Class="NELpizza.View.ManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewmodels="clr-namespace:NELpizza.ViewModels.Views"
             xmlns:local="clr-namespace:NELpizza.Helpers"
             mc:Ignorable="d">

	<UserControl.DataContext>
		<viewmodels:ManagerViewModel />
	</UserControl.DataContext>

	<UserControl.Resources>
		<local:ByteArrayToImageConverter x:Key="ByteArrayToImageConverter" />
	</UserControl.Resources>

	<Grid Margin="20">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<!-- Input Section -->
		<StackPanel Orientation="Vertical" Margin="0,0,0,10" Grid.Row="0">
			<TextBox Text="{Binding Naam, UpdateSourceTrigger=PropertyChanged}"
                     materialDesign:HintAssist.Hint="Pizza Naam" Margin="0,0,0,10" />
			<TextBox Text="{Binding Prijs, UpdateSourceTrigger=PropertyChanged}"
                     materialDesign:HintAssist.Hint="Prijs (€)" Margin="0,0,0,10" />
			<TextBox Text="{Binding Beschrijving, UpdateSourceTrigger=PropertyChanged}"
                     materialDesign:HintAssist.Hint="Beschrijving" Margin="0,0,0,10" />

			<!-- Image Section -->
			<StackPanel Orientation="Vertical" Margin="0,10,0,10">
				<Button Content="Upload Image" Command="{Binding UploadImageCommand}" />
				<Image Source="{Binding Image, Converter={StaticResource ByteArrayToImageConverter}}"
                       Height="150" Margin="0,10,0,0" Stretch="Uniform" />
			</StackPanel>

			<!-- Action Buttons -->
			<StackPanel Orientation="Horizontal" Margin="0,10,0,0">
				<Button Content="Toevoegen" Command="{Binding AddPizzaCommand}" Margin="0,0,10,0" />
				<Button Content="Bijwerken" Command="{Binding UpdatePizzaCommand}" IsEnabled="{Binding CanModifyOrDelete}" Margin="0,0,10,0" />
				<Button Content="Verwijderen" Command="{Binding DeletePizzaCommand}" IsEnabled="{Binding CanModifyOrDelete}" />
			</StackPanel>
		</StackPanel>

		<!-- Pizza List -->
		<ListBox ItemsSource="{Binding Pizzas}" SelectedItem="{Binding SelectedPizza}" Grid.Row="1">
			<ListBox.ItemTemplate>
				<DataTemplate>
					<materialDesign:Card Margin="5" Padding="10" materialDesign:ElevationAssist.Elevation="Dp2">
						<StackPanel>
							<TextBlock FontWeight="Bold" Text="{Binding Naam}" />
							<TextBlock Text="{Binding Prijs, StringFormat=Prijs: €{0:F2}}" />
							<TextBlock Foreground="Gray" Text="{Binding Beschrijving}" />
							<Image Source="{Binding Image, Converter={StaticResource ByteArrayToImageConverter}}"
                                   Height="75" Stretch="Uniform" />
						</StackPanel>
					</materialDesign:Card>
				</DataTemplate>
			</ListBox.ItemTemplate>
		</ListBox>
	</Grid>
</UserControl>
