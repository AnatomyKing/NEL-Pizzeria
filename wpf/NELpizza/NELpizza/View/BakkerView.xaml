<UserControl x:Class="NELpizza.View.BakkerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewmodels="clr-namespace:NELpizza.ViewModels.Views"
             mc:Ignorable="d">

    <UserControl.DataContext>
        <viewmodels:BakkerViewModel />
    </UserControl.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Incoming Orders Section -->
        <StackPanel Orientation="Vertical" Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Incoming Orders (Besteld)" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
            <ListBox ItemsSource="{Binding IncomingOrders}" SelectedItem="{Binding SelectedOrder}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <materialDesign:Card Margin="5" Padding="10" materialDesign:ElevationAssist.Elevation="Dp2">
                            <StackPanel Orientation="Vertical">
                                <TextBlock FontWeight="Bold">
                                    <Run Text="Order ID: "/>
                                    <Run Text="{Binding Id}"/>
                                </TextBlock>
                                <TextBlock>
                                    <Run Text="Customer: "/>
                                    <Run Text="{Binding Klant.Naam}"/>
                                </TextBlock>
                                <TextBlock Foreground="Gray">
                                    <Run Text="Date: "/>
                                    <Run Text="{Binding Datum, StringFormat=dd-MM-yyyy HH:mm}"/>
                                </TextBlock>
                                <TextBlock Foreground="Blue">
                                    <Run Text="Status: "/>
                                    <Run Text="{Binding Status}"/>
                                </TextBlock>

                                <!-- Pizza Details -->
                                <ItemsControl ItemsSource="{Binding Bestelregels}" Margin="0,10,0,0">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border BorderBrush="LightGray" BorderThickness="1" Padding="5" Margin="0,5,0,5">
                                                <StackPanel Orientation="Vertical">
                                                    <TextBlock FontWeight="SemiBold">
                                                        <Run Text="Pizza: "/>
                                                        <Run Text="{Binding Pizza.Naam}"/>
                                                    </TextBlock>
                                                    <TextBlock>
                                                        <Run Text="Size: "/>
                                                        <Run Text="{Binding Afmeting}"/>
                                                        <Run Text=", Quantity: "/>
                                                        <Run Text="{Binding Aantal}"/>
                                                    </TextBlock>

                                                    <!-- Ingredients Details -->
                                                    <ItemsControl ItemsSource="{Binding BestelregelIngredients}" Margin="10,5,0,0">
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <TextBlock FontStyle="Italic">
                                                                    <Run Text="• Ingredient: "/>
                                                                    <Run Text="{Binding Ingredient.Naam}"/>
                                                                    <Run Text=", Qty: "/>
                                                                    <Run Text="{Binding Quantity}"/>
                                                                </TextBlock>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                                <Button Content="Prepare" 
                                        Command="{Binding DataContext.ProcessOrderCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                        CommandParameter="{Binding}" 
                                        Margin="0,10,0,0"/>
                            </StackPanel>
                        </materialDesign:Card>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>

        <!-- Preparation Section -->
        <StackPanel Orientation="Vertical" Grid.Row="1" Margin="0,0,0,20">
            <TextBlock Text="Preparation (Bereiden)" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
            <ListBox ItemsSource="{Binding PreparingOrders}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <materialDesign:Card Margin="5" Padding="10" materialDesign:ElevationAssist.Elevation="Dp2">
                            <StackPanel Orientation="Vertical">
                                <TextBlock FontWeight="Bold">
                                    <Run Text="Order ID: "/>
                                    <Run Text="{Binding Id}"/>
                                </TextBlock>
                                <TextBlock>
                                    <Run Text="Customer: "/>
                                    <Run Text="{Binding Klant.Naam}"/>
                                </TextBlock>
                                <TextBlock Foreground="Gray">
                                    <Run Text="Date: "/>
                                    <Run Text="{Binding Datum, StringFormat=dd-MM-yyyy HH:mm}"/>
                                </TextBlock>
                                <TextBlock Foreground="Orange">
                                    <Run Text="Status: "/>
                                    <Run Text="{Binding Status}"/>
                                </TextBlock>
                            </StackPanel>
                        </materialDesign:Card>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>

        <!-- Furnace Section -->
        <StackPanel Orientation="Vertical" Grid.Row="2">
            <TextBlock Text="Furnace (Inoven)" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
            <ListBox ItemsSource="{Binding FurnaceOrders}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <materialDesign:Card Margin="5" Padding="10" materialDesign:ElevationAssist.Elevation="Dp2">
                            <StackPanel Orientation="Vertical">
                                <TextBlock FontWeight="Bold">
                                    <Run Text="Order ID: "/>
                                    <Run Text="{Binding Id}"/>
                                </TextBlock>
                                <TextBlock>
                                    <Run Text="Customer: "/>
                                    <Run Text="{Binding Klant.Naam}"/>
                                </TextBlock>
                                <TextBlock Foreground="Gray">
                                    <Run Text="Date: "/>
                                    <Run Text="{Binding Datum, StringFormat=dd-MM-yyyy HH:mm}"/>
                                </TextBlock>
                                <TextBlock Foreground="Green">
                                    <Run Text="Status: "/>
                                    <Run Text="{Binding Status}"/>
                                </TextBlock>
                            </StackPanel>
                        </materialDesign:Card>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Grid>
</UserControl>