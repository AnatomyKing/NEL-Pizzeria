<Window x:Class="NELpizza.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:NELpizza.View"
        xmlns:viewmodels="clr-namespace:NELpizza.ViewModels"
        xmlns:mainvm="clr-namespace:NELpizza.ViewModels"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Style="{StaticResource MaterialDesignWindow}"
        mc:Ignorable="d"
        Title="Custom Model Manager" Height="700" Width="1000">

    <Window.DataContext>
        <mainViewModel:MainViewModel/>
    </Window.DataContext>
    
    <DockPanel>
        <!-- Navigation Menu -->
        <StackPanel DockPanel.Dock="Left" Background="#2C3E50" Width="200">
            <Button Content="Baker Orders" Command="{Binding NavigateCommand}" CommandParameter="BakerOrdersViewModel" Margin="10"/>
            <Button Content="Delivery Orders" Command="{Binding NavigateCommand}" CommandParameter="DeliveryOrdersViewModel" Margin="10"/>
            <Button Content="TrackTrace" Command="{Binding NavigateCommand}" CommandParameter="TrackTraceViewModel" Margin="10"/>
            <Button Content="Menu Management" Command="{Binding NavigateCommand}" CommandParameter="MenuManagementViewModel" Margin="10"/>
            <Button Content="Customer Management" Command="{Binding NavigateCommand}" CommandParameter="CustomerManagementViewModel" Margin="10"/>
        </StackPanel>

        <!-- Dynamic Content -->
        <ContentControl Content="{Binding CurrentView}" Margin="10"/>
    </DockPanel>
</Window>
